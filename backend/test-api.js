async function test() { try { const login = await fetch('http://localhost:3000/api/auth/login', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({email: 'admin@test.com', password: 'password123'}) }); const loginBody = await login.json(); const token = loginBody.access_token || loginBody.token || loginBody.accessToken; const res = await fetch('http://localhost:3000/api/families/5', { method: 'PATCH', headers: {'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token}, body: JSON.stringify({settings: {hero: {title: 'Testing Marquee'}, marqueeItems: ['Hello World']}}) }); const data = await res.json(); console.log('Response:', data); } catch(e) { console.error(e.message); } } test();
